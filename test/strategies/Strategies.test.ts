import { expect } from "chai";
import { ethers } from "hardhat";

describe("Strategies", function () {
    describe("CurveStrategy", function () {
        it("should deposit into pool", async function () {
            // Integration test skeleton
        });

        it("should harvest CRV", async function () {
            // Harvest logic test
        });
    });

    describe("AaveStrategy", function () {
        it("should supply and earn aTokens", async function () {
            // Aave integration
        });
    });
});
